/* pages/patient/register/register.wxss */
page {
  background: linear-gradient(180deg, #F0F7FF 0%, #FFFFFF 100%);
  min-height: 100vh;
}

.page-container {
  padding: 32rpx;
  min-height: 100vh;
}

/* 表单卡片 */
.form-card {
  background: #FFFFFF;
  border-radius: 20rpx;
  padding: 32rpx;
  box-shadow: 0 8rpx 32rpx rgba(0, 0, 0, 0.06);
}

.form-title {
  font-size: 32rpx;
  font-weight: bold;
  color: #1A1A1A;
  margin-bottom: 32rpx;
  padding-bottom: 16rpx;
  border-bottom: 2rpx solid #F0F0F0;
  text-align: center;
}

/* 表单项 */
.form-item {
  margin-bottom: 28rpx;
}

.form-row {
  display: flex;
  gap: 20rpx;
  margin-bottom: 28rpx;
}

.half-item {
  flex: 1;
  margin-bottom: 0;
}

.label-row {
  display: flex;
  align-items: center;
  margin-bottom: 12rpx;
}

.label {
  font-size: 28rpx;
  color: #333333;
  font-weight: 500;
}

.required {
  color: #FF4D4F;
  font-size: 28rpx;
  margin-left: 6rpx;
}

/* 输入框 */
.input {
  width: 100%;
  height: 80rpx;
  background: #F7F8FA;
  border: 2rpx solid #E8E8E8;
  border-radius: 12rpx;
  padding: 0 20rpx;
  font-size: 28rpx;
  color: #333333;
  box-sizing: border-box;
  transition: all 0.3s ease;
}

.input:focus {
  border-color: #7BCA9A;
  background: #FFFFFF;
}

.input::placeholder {
  color: #BBBBBB;
}

/* 性别选择器 */
.gender-selector {
  display: flex;
  gap: 12rpx;
}

.gender-btn {
  flex: 1;
  height: 80rpx;
  background: #F7F8FA;
  border: 2rpx solid #E8E8E8;
  border-radius: 12rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all 0.3s ease;
}

.gender-btn.active {
  background: linear-gradient(135deg, #E8F4FD 0%, #FFFFFF 100%);
  border-color: #7BCA9A;
  border-width: 3rpx;
  box-shadow: 0 4rpx 12rpx rgba(123, 202, 154, 0.15);
}

.gender-text {
  font-size: 28rpx;
  color: #666666;
  font-weight: 500;
}

.gender-btn.active .gender-text {
  color: #7BCA9A;
  font-weight: bold;
}

.gender-btn:active {
  transform: scale(0.98);
}

/* 日期选择器 */
.date-selector {
  height: 80rpx;
  background: #F7F8FA;
  border: 2rpx solid #E8E8E8;
  border-radius: 12rpx;
  padding: 0 20rpx;
  display: flex;
  align-items: center;
  justify-content: flex-start;
  transition: all 0.3s ease;
  box-sizing: border-box;
}

.date-selector:active {
  background: #FFFFFF;
  border-color: #7BCA9A;
}

.date-text {
  font-size: 28rpx;
  color: #333333;
  flex: 1;
  display: block;
  line-height: 28rpx;
  transform: translateY(-10rpx);
}

.date-text.placeholder {
  color: #BBBBBB;
}


/* 手机号输入区域 */
.phone-input-wrapper {
  position: relative;
  display: flex;
  align-items: center;
  gap: 12rpx;
}

.phone-input {
  flex: 1;
  min-width: 0;
  width: auto;
}

.auth-button {
  height: 80rpx !important;
  width: 130rpx !important;
  min-width: 130rpx !important;
  max-width: 130rpx !important;
  padding: 0 8rpx !important;
  background: linear-gradient(135deg, #7BCA9A 0%, #8DD5B0 100%) !important;
  color: #FFFFFF !important;
  font-size: 24rpx !important;
  border-radius: 12rpx;
  border: none;
  white-space: nowrap;
  line-height: 80rpx;
  box-shadow: 0 4rpx 12rpx rgba(123, 202, 154, 0.25);
  font-weight: 500;
  flex-shrink: 0;
}

.auth-button::after {
  border: none;
}

.auth-button:active {
  transform: scale(0.98);
}

/* 治疗组选择器 */
.team-selector {
  height: 80rpx;
  background: #F7F8FA;
  border: 2rpx solid #E8E8E8;
  border-radius: 12rpx;
  padding: 0 20rpx;
  display: flex;
  align-items: center;
  justify-content: space-between;
  transition: all 0.3s ease;
  box-sizing: border-box;
}

.team-selector:active {
  background: #FFFFFF;
  border-color: #7BCA9A;
}

.team-value {
  font-size: 28rpx;
  color: #333333;
  flex: 1;
  display: block;
  line-height: 28rpx;
  transform: translateY(-10rpx);
}

.team-value.placeholder {
  color: #BBBBBB;
}

.selector-arrow {
  font-size: 40rpx;
  color: #BBBBBB;
  font-weight: 300;
  display: block;
  line-height: 40rpx;
  transform: translateY(-2rpx);
}

.team-hint {
  font-size: 22rpx;
  color: #999999;
  margin-top: 8rpx;
  line-height: 1.4;
}

/* 医生列表弹窗 */
.modal-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.5);
  z-index: 1000;
  display: flex;
  align-items: flex-end;
  justify-content: center;
}

.modal-container {
  width: 100%;
  max-height: 70vh;
  background: #FFFFFF;
  border-radius: 20rpx 20rpx 0 0;
  display: flex;
  flex-direction: column;
  overflow: hidden;
  animation: slideUp 0.3s ease;
}

@keyframes slideUp {
  from {
    transform: translateY(100%);
    opacity: 0;
  }
  to {
    transform: translateY(0);
    opacity: 1;
  }
}

.modal-header {
  padding: 32rpx;
  border-bottom: 2rpx solid #F0F0F0;
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.modal-title {
  font-size: 32rpx;
  font-weight: bold;
  color: #1A1A1A;
}

.modal-close {
  font-size: 48rpx;
  color: #999999;
  line-height: 1;
  padding: 0 8rpx;
}

.modal-body {
  flex: 1;
  overflow: hidden;
}

.doctor-list {
  height: 600rpx;
  padding: 16rpx 0;
}

.doctor-item {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 24rpx 32rpx;
  border-bottom: 2rpx solid #F7F8FA;
  transition: all 0.3s ease;
}

.doctor-item:active {
  background: #F7F8FA;
}

.doctor-item.selected {
  background: linear-gradient(135deg, #E8F4FD 0%, #FFFFFF 100%);
}

.doctor-info {
  display: flex;
  align-items: center;
  gap: 20rpx;
  flex: 1;
}

.doctor-avatar {
  width: 88rpx;
  height: 88rpx;
  background: linear-gradient(135deg, #7BCA9A 0%, #5BA87D 100%);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.avatar-icon {
  font-size: 48rpx;
  line-height: 1;
}

.doctor-details {
  flex: 1;
}

.doctor-name {
  font-size: 30rpx;
  font-weight: bold;
  color: #1A1A1A;
  margin-bottom: 8rpx;
}

.doctor-meta {
  display: flex;
  gap: 16rpx;
  margin-bottom: 6rpx;
}

.doctor-title {
  font-size: 24rpx;
  color: #7BCA9A;
  background: #E8F4FD;
  padding: 4rpx 12rpx;
  border-radius: 6rpx;
}

.doctor-dept {
  font-size: 24rpx;
  color: #666666;
}

.doctor-hospital {
  font-size: 22rpx;
  color: #999999;
}

.check-icon {
  font-size: 48rpx;
  color: #7BCA9A;
  line-height: 1;
  font-weight: bold;
  flex-shrink: 0;
}

/* 提交按钮 */
.submit-btn {
  width: 100%;
  height: 100rpx;
  background: linear-gradient(135deg, #7BCA9A 0%, #5BA87D 100%);
  color: #FFFFFF;
  font-size: 32rpx;
  font-weight: 500;
  border-radius: 24rpx;
  margin-top: 32rpx;
  border: none;
  box-shadow: 0 12rpx 32rpx rgba(123, 202, 154, 0.35);
  transition: all 0.3s ease;
  display: flex;
  align-items: center;
  justify-content: center;
  text-align: center;
  padding: 0;
  position: relative;
  overflow: hidden;
}

.submit-btn:active {
  transform: translateY(2rpx);
  box-shadow: 0 8rpx 24rpx rgba(123, 202, 154, 0.3);
}

.submit-btn::after {
  border: none;
}

.submit-btn[loading] {
  background: linear-gradient(135deg, #94BEE5 0%, #7AA3D1 100%);
}

/* 底部说明 */
.footer-note {
  text-align: center;
  margin-top: 24rpx;
}

.note-text {
  font-size: 24rpx;
  color: #999999;
  line-height: 1.5;
}

